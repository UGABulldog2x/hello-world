# hello-world

This is my first repository.  Learning is fundamental.

I like humans and dogs.  They are both beings on the planet Earth.

The Sun is a star.  It is my favorite star.

## Usage

```bash
python3 hello.py [name]
```

Prints `Hello, World!` if no name is given, or `Hello, <name>!` otherwise.

## Running tests

```bash
pip install pytest
pytest
```

Tests also run automatically on every push and pull request via GitHub Actions.
